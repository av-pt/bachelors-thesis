% Thesis checklist:
%All done!

%X Consistent periods after fable and figure descriptions?
%X Non-breaking spaces before \ref{} etc.?
%X % % figure~\ref{fig:...} and table~\ref{tab:...} everywhere?
%X Possessive s: authors' author's
%X Correct quotation marks: ``'' vs ""

%X Spellcheck: dataset / data set -> dataset
%X PAN20 / PAN2020 -> PAN 2020
%X naive / naïve -> naive
%X gutenberg / Gutenberg -> Gutenberg
%X fanfiction / Fanfiction / fan-fiction -> Fanfiction
%X $n$-grams / n-grams -> $n$-grams
%X artifact / artefact -> artifact
%X -- (gedankenstrich wie?) -> ---
%X precision / recall capitalization -> lower case
%X Check: measure / score -> leave like it is
%X possessive apostrophes -> check
%X Gutenberg etc. cursive or not? -> No, but capitalized
%X Table Figure Section groß oder klein schreiben? -> Groß: Section 3.4 ist ein Eigenname

%X Check if transcription systems are uniformly marked ($IPA$ vs. IPA)
%X -> Only when we talk about the actual transcription we use, it should be put into math mode
%X maybe also say that somewhere
%X \textit instead of $$ for transcription systems, VSSF, and scores

%X Alphabet sizes in table!
%X Footnote in table
%X IPA examples should maybe be inclosed in [] or // instead of ""
%X Fix capital letters in bibtex
%X Bei bibliography am ende dazu: " Cited on pages:" -> Nope.
%X Single enumeration of tables and figures -> Nope!
%X Check if uncertainty interval zeile ist zu lang, p.37
%X After everything else: Check for empty space optimization.
%X Change base repository